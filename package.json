{
  "name": "adera-hybrid-app",
  "version": "1.0.0",
  "private": true,
  "description": "Adera Hybrid App - Logistics (PTP) and E-Commerce (Shop) platform for Addis Ababa",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean",
    "type-check": "turbo run type-check"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "turbo": "^2.5.8",
    "typescript": "^5.0.0"
  },
  "packageManager": "pnpm@10.18.1",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "dependencies": {
    "adera-ptp": "link:apps\\adera-ptp",
    "zustand": "4.5.2"
  }
}
